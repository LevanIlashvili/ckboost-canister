# Network configuration
VITE_DFX_NETWORK=local

# Local development canister IDs (replace with your local canister IDs)
VITE_CANISTER_ID_BACKEND=
VITE_CANISTER_ID_FRONTEND=

# For production, you would use:
# VITE_DFX_NETWORK=ic
# VITE_CANISTER_ID_BACKEND=<your-production-backend-canister-id>
# VITE_CANISTER_ID_FRONTEND=<your-production-frontend-canister-id> 